fn main() {
    //1
    // let mut x = zero_fuel(50, 25, 2);
    // println!("{}", x);
    // x = zero_fuel(100, 50, 1);
    // println!("{}", x);

    //2
    // let mut y = count_red_beads(0);
    // println!("{}", y);
    // y = count_red_beads(3);
    // println!("{}", y);
    // y = count_red_beads(5);
    // println!("{}", y);
}

// fn zero_fuel(distance_to_pump: u32, mpg: u32, gallons: u32) -> bool {
//     // unimplemented!();
//     if distance_to_pump <= mpg * gallons {
//         true
//     } else {
//         false
//     }

//     //other examples
//     //distance_to_pump <= mpg * gallons
//     //return b * c >= a
// }

// fn count_red_beads(n: u32) -> u32 {
// if n < 2 {
//     return 0;
// } else if n == 2 {
//     return 2;
// } else {
//     return (n - 1) * 2;
// }

//other examples
// match n {
//     0 | 1 => 0,
//     _ => (n - 1) * 2,
// }

// 2 * n.checked_sub(1).unwrap_or(0)

// match n {
// n if n > 1 => (n - 1) * 2,
// _ => 0
// }
// }
